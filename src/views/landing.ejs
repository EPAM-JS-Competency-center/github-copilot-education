<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link rel="stylesheet" href="/css/style.css">
    <script>
        const handleKeyDown = (event) => {
            if (event.key === 'Enter' || event.key === ' ') {
                window.location.href = '/auth/google';
            }
        };
    </script>
    <style>
        .landing {
            display: flex;
            min-height: 70vh;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 16px;
        }
    </style>
    </head>
<body>
    <%- include('./partials/header.ejs') %>
    <main>
        <section class="landing" aria-labelledby="welcome-title">
            <h1 id="welcome-title">Welcome</h1>
            <p>Please sign in to manage users.</p>
            <a href="/auth/google" class="btn btn-primary" role="button" aria-label="Sign in with Google" tabindex="0" onkeydown="handleKeyDown(event)">Sign in with Google</a>
        </section>
    </main>
    <%- include('./partials/footer.ejs') %>
</body>
</html>


